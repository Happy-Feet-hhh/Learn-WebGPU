/**
 * Color - RGB 颜色类
 *
 * 表示一个 RGB 颜色值，每个分量范围为 0.0 ~ 1.0（浮点数）。
 * 例如：(1, 1, 1) 为白色，(0, 0, 0) 为黑色，(1, 0, 0) 为红色。
 *
 * 注意：不包含 alpha（透明度）通道，如需透明度需额外处理。
 * 默认值为白色 (1, 1, 1)，即不施加任何颜色偏移。
 */
export class Color
{
    /**
     * @param r - 红色分量，默认 1（满值）
     * @param g - 绿色分量，默认 1（满值）
     * @param b - 蓝色分量，默认 1（满值）
     */
    constructor(public r = 1, public g = 1, public b = 1)
    {
        
    }
}
